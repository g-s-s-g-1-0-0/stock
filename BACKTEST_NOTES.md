# 백테스트 참고 노트 — 전략 신호 규칙

백테스트 스크립트(`backtest_*.py`)가 라이브 시스템(웹 파이프라인 / GAS)과 동일한 기준으로
진입·추가매수를 집계하도록, 핵심 규칙과 최근 수정 사항을 여기에 기록한다.
전략 상세 정의는 `CONTEXT.md`가 단일 출처(source of truth)이며, 이 문서는 그 규칙 중
"신호 선택" 부분을 백테스트 관점에서 요약·보강한 것이다.

## 1. 단일 최고 우선순위 규칙 (가장 중요)

우선순위는 **A > B > C > D > E > F > G**.

한 회차(평가 시점)에 여러 전략이 동시에 조건을 충족해도 **우선순위가 가장 높은 전략
하나만** 신호로 채택한다. 이는 다음 두 경우 모두에 적용된다.

- **신규 진입**: 동시 충족 시 최상위 1개 전략만 진입.
- **보유 포지션 추가매수 슬롯**: 동시 충족 시 최상위 1개 전략만 슬롯을 연다.
  (예: E·F가 같은 회차에 동시 충족 → E만 채택, F는 같은 회차에 진입하지 않음)

> 백테스트 구현 시: 매 회차마다 후보 전략을 우선순위 순으로 prescan 하고,
> 조건을 만족하는 첫 전략에서 멈춰 **한 회차당 한 전략만** 진입/추가매수로 집계할 것.
> 여러 전략을 독립적으로 평가해 동시에 카운트하면 라이브와 결과가 달라진다.

## 2. 라이브 코드의 적용 지점

| 영역 | 파일 | 적용 방식 |
|------|------|-----------|
| 웹 신규 진입 | `rules.py` (`evaluate_buy_condition`), `pipeline.py` | `strategyType` 단일 반환, `entrySignalCodes` 1개로 정리 |
| GAS 신규 진입 | `updateInvestmentOpinion.gs` (`evaluateBuyCondition`) | 그룹 상호배타 가드(`!entryGroupA && !entryGroupB ...`) → 단일 `strategyType` |
| GAS 추가매수 슬롯 | `trackChanges.gs` (슬롯 루프 + `evaluateSlotEntry`) | 회차마다 후보 전략 우선순위 prescan 후 **최상위 1개만** 슬롯 생성 |

## 3. 최근 수정 이력

- **웹 파이프라인 (어제)**: `pipeline.py`에서 동시 충족 시 `entrySignalCodes`에 여러 전략이
  들어가 같은 종목이 E·F 등 복수 신호로 잡히던 문제를 단일 최상위 전략만 남기도록 수정.
  또한 보유 포지션이 추가매수 조건을 못 채워도 신규 진입 조건이 유지되면 `매수` 의견을
  내리지 않도록 보정(모노토닉 보유 의견).
- **`trackChanges.gs` (이번 변경, 커밋 `226c992`)**: 추가매수 슬롯 생성 루프가 전략별로
  독립 평가하여 한 회차에 복수 슬롯(E·F)을 동시에 열 수 있던 문제를, 우선순위 prescan으로
  최상위 1개 전략만 슬롯을 열도록 수정.
- **`updateInvestmentOpinion.gs` (이번 점검 — 수정 없음)**: 신규 진입은 이미
  `buy.strategyType`(단일), 보유는 `saved.strategyType`만 사용하고, 투자의견이 종목당
  한 칸(문자열)이라 복수 신호가 생길 구조가 아님. 보유 의견은 `isHolding=true`의 완화 조건으로
  평가되어 청산 전까지 `매수`를 유지하므로 웹에서 고쳤던 보유 의견 강등 문제도 해당되지 않음.
  → **추가 수정 불필요.**

## 4. 보유 추가매수의 추가 게이트 (백테스트 시 같이 반영)

위 단일 우선순위 외에, 보유 중 추가매수에는 다음 게이트가 더 붙는다 (`CONTEXT.md` §4 참조).

- 현재가가 기존 진입가 대비 `-10% 이상` 추가 하락
- 직전 진입/슬롯 대비 `10거래일` 경과
- 현재 전략의 매수 조건 재충족, 슬롯 한도 이내
- E/F 계열 교차 추가매수는 같은 새 전략 신호가 `2회 연속` 확인되어야 함
